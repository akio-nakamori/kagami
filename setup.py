#!/usr/bin/env python
# kagami

try:
    from setuptools import setup
except ImportError:
    from distutils.core import setup

setup(
    name='kagami',
    version="0.1",
    description='',
    long_description='',
    author='bigretromike',
    url='',
    packages=["kagami", ],
    test_suite='tests',
    install_requires=[],
    license='MIT'
)
